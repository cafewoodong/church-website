# 설교 블로그 → 교회 홈페이지 통합 인수인계

> 이 파일을 다른 노트북(church-website 있는 곳)에서 열고 순서대로 따라하세요.

---

## 현재 상황 요약

| 항목 | 내용 |
|------|------|
| sermon-blog GitHub | https://github.com/cafewoodong/sermon-blog |
| church-website GitHub | https://github.com/cafewoodong/church-website |
| 빅터스 노트북 | sermon-blog 완성 + GitHub 업로드 완료 ✅ |
| 다른 노트북 | church-website 소스코드 있음 — 여기서 통합 작업 |

---

## sermon-blog가 하는 일

```
관리자가 유튜브 링크 붙여넣기 + 버튼 클릭
        ↓
① 유튜브 자막 자동 추출 (youtube-transcript)
        ↓
② Gemini AI가 서론·본론·결론 구조로 요약
        ↓
③ Supabase sermon_summaries 테이블에 저장
        ↓
④ /sermon/[id] 블로그 페이지 자동 생성
```

---

## sermon-blog 구조 (GitHub에서 받으면 이렇게 생겼음)

```
sermon-blog/
├── app/
│   ├── page.tsx              ← 메인 (최신 설교 목록)
│   ├── layout.tsx
│   ├── admin/page.tsx        ← 관리자 등록 폼 (/admin)
│   ├── sermon/page.tsx       ← 설교 목록 (/sermon)
│   └── sermon/[id]/page.tsx  ← 설교 상세 (/sermon/[id])
├── components/
│   ├── SermonCard.tsx
│   ├── YoutubeEmbed.tsx
│   └── AnimatedSection.tsx
├── lib/
│   ├── supabase.ts           ← Supabase 클라이언트
│   ├── types.ts              ← 타입 정의
│   └── demo.ts               ← 데모 데이터 (키 없을 때 사용)
└── app/api/summarize/route.ts ← 핵심 API (자막추출→AI요약→DB저장)
```

---

## STEP 1 — sermon-blog 코드를 이 노트북으로 가져오기

church-website 소스 폴더 옆에서 실행:

```bash
git clone https://github.com/cafewoodong/sermon-blog.git
```

---

## STEP 2 — .env.local 파일 만들기

`sermon-blog/` 폴더 안에 `.env.local` 파일을 새로 만들고 아래 내용 입력:

```
NEXT_PUBLIC_SUPABASE_URL=여기에_Supabase_프로젝트_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=여기에_anon_key
SUPABASE_SERVICE_ROLE_KEY=여기에_service_role_key
GEMINI_API_KEY=여기에_Gemini_API_키
ADMIN_PASSWORD=여기에_관리자_비밀번호_설정
```

> Supabase 키 위치: https://supabase.com → 프로젝트 → Settings → API
> Gemini 키 위치: https://aistudio.google.com → Get API key

---

## STEP 3 — Supabase 테이블 만들기

Supabase 대시보드 → SQL Editor에서 실행:

```sql
create table sermon_summaries (
  id uuid primary key default gen_random_uuid(),
  youtube_url text not null,
  video_id text,
  title text,
  scripture text,
  key_verse text,
  one_line text,
  summary jsonb,
  thumbnail_url text,
  category text,
  created_at timestamptz default now()
);

alter table sermon_summaries enable row level security;

create policy "public read" on sermon_summaries
  for select using (true);
```

---

## STEP 4 — sermon-blog 단독 실행 테스트

```bash
cd sermon-blog
npm install
npm run dev
```

브라우저에서 http://localhost:3000/admin 열고 실제 설교 유튜브 링크로 테스트.
정상이면 /sermon/[id] 페이지가 자동 생성됨.

---

## STEP 5 — church-website와 통합

아래 중 하나를 선택:

### 방법 A (권장) — 메뉴에 링크만 연결
church-website 네비게이션에 "설교 말씀" 메뉴 추가 → sermon-blog URL로 이동.
sermon-blog를 Vercel에 배포하면 URL이 생김.

### 방법 B — 같은 Supabase 공유
church-website와 sermon-blog가 **같은 Supabase 프로젝트**를 쓰면
church-website에서도 sermon_summaries 테이블의 최신 설교를 불러올 수 있음.
church-website 메인 페이지에 최신 설교 1~3개 카드를 표시하는 식으로 사용.

### 방법 C — sermon-blog를 church-website 안에 합치기
두 프로젝트를 하나로 합치는 방법. 작업량 많음. 나중에 해도 됨.

---

## STEP 6 — sermon-blog Vercel 배포 (방법 A 선택 시)

```bash
# Vercel CLI 설치
npm i -g vercel

# sermon-blog 폴더에서 배포
cd sermon-blog
vercel
```

배포 중 환경변수 입력 안내가 나옴 → STEP 2의 키들을 입력.
완료되면 https://sermon-blog-xxx.vercel.app 같은 URL이 생김.

---

## 설교 카테고리 (3종)

| 카테고리 | 색상 |
|----------|------|
| 주일설교 | 파란색 |
| 수요기도회 | 보라색 |
| 새벽설교 | 노란색 |

---

## 요약 JSON 구조 (AI가 이 형태로 반환)

```json
{
  "title": "설교 제목",
  "scripture": "마태복음 5:1-12",
  "key_verse": "핵심 구절 한 줄",
  "one_line": "설교 전체를 한 문장으로",
  "sections": [
    { "heading": "서론", "content": "요약 내용", "icon": "BookOpen" },
    { "heading": "본론", "content": "요약 내용", "icon": "MessageSquare" },
    { "heading": "결론", "content": "요약 내용", "icon": "Sunrise" }
  ],
  "application": ["적용점1", "적용점2", "적용점3"],
  "prayer": "결단 기도문"
}
```

---

## 막히면 물어볼 것들

- Supabase 키를 어디서 복사하는지 모르겠으면 → Claude Code에게 물어보기
- church-website에서 sermon-blog 데이터 불러오는 코드 → Claude Code에게 "sermon_summaries 테이블에서 최신 3개 설교 가져오는 코드 만들어줘" 요청
- Vercel 배포 막힘 → Claude Code에게 에러 메시지 붙여넣기

---

## 기술 스택 (sermon-blog)

- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS (church-navy `#243B53`, church-gold `#d6a84f` 커스텀 컬러)
- framer-motion (스크롤 애니메이션)
- lucide-react (아이콘)
- @supabase/supabase-js
- youtube-transcript
- Gemini 2.5 Flash API
